const ActionHistory = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "flex-end",
        alignItems: "center",
      }}
    >
      <h1>Action History</h1>
    </div>
  );
};

export default ActionHistory;
